# GoFundBot 优化与待完善功能清单

> 生成时间：2026-06-28
> 排除项：用户身份认证/多用户体系（不纳入本次范围）

---

## 1. 安全加固

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 1.1 | 无速率限制 | 全栈 (Flask / DataService) 所有 API 均无限流，可被滥用 | Flask-Limiter 中间件；DataService express-rate-limit |
| 1.2 | 无输入校验框架 | 仅 `assertCode()` 做基础校验，无请求体验证 | marshmallow / pydantic 做请求模型校验 |
| 1.3 | `.env` 密钥已提交到仓库 | LLM / 搜索 API 密钥明文存在于 repo 内 | `.gitignore` 排除 `.env`；仅保留 `.env.example` 不含真实密钥 |
| 1.4 | CORS 默认全放行 | Backend `flask-cors` 和 DataService `cors` 均未限制来源 | 生产环境配置白名单 |
| 1.5 | 无 CSRF 防护 | POST/PUT/DELETE 端点无 token 校验 | Flask-WTF CSRF 或无状态 API 方案 (SameSite Cookie) |

## 2. 生产就绪度

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 2.1 | 无结构化日志 | Backend 全线 `print()`；无日志级别、无请求追踪 | Python `logging` 模块 + JSON 格式；统一 `request_id` 链路追踪 |
| 2.2 | 无 Backend 健康检查 | 仅 DataService 有 `/api/health`；Backend 缺少 | 添加 `/health` 端点，检查 DB + DataService 连通性 |
| 2.3 | 无优雅关闭 | Flask 后台线程 (`daemon=True`) 退出时不等待 | 注册 `atexit` / signal handler，确保任务完成或超时后再退出 |
| 2.4 | 无监控/报警 | 无 Prometheus metrics、无 Sentry 错误追踪、无运行状态面板 | 接入 Sentry；暴露 `/metrics` 端点 |
| 2.5 | 无 API 版本化 | 破坏性变更无法管理 | URL 前缀版控 (`/api/v1/...`) 或用请求头 |
| 2.6 | 无 API 文档 | 67 个端点无 Swagger/OpenAPI 规范 | Flask-RESTX / flasgger 自动生成 OpenAPI 文档 |

## 3. 性能优化

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 3.1 | 无前端状态管理 | 组件各自独立调 API，重复请求（如 `FundWatchlist` 和 `FundSearch` 各自 fetch） | 引入 Pinia；共享状态避免重复请求 |
| 3.2 | 无 HTTP 缓存头 | Flask 响应无 `ETag` / `Cache-Control` / `Last-Modified` | 按资源类型设置合理 TTL |
| 3.3 | 图表无节流重渲染 | `FundDetail` / `MarketOverview` 每次数据更新都重绘 ECharts | `requestAnimationFrame` 或 `debounce` 合并渲染 |
| 3.4 | 前端无骨架屏 | 加载状态仅为简单 spinner | Vue Suspense + skeleton 组件 |
| 3.5 | 生产环境无 Gzip/Brotli | 静态资源未压缩传输 | Nginx 反向代理压缩；或 Flask-Compress |
| 3.6 | SQLite 单写入者瓶颈 | 批量筛选更新时写锁阻塞 | WAL 模式 (`PRAGMA journal_mode=WAL`)；考虑超时重试 |
| 3.7 | DataService 内存缓存上限 2000 条 | LRU 淘汰可能丢弃有价值数据 | 增大上限或对长 TTL 数据做文件持久化 |
| 3.8 | 部分端点无分页 | `/api/watchlist` 返回全量 | 统一增加 `page`/`page_size` 参数 |

## 4. 测试体系

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 4.1 | 仅 1 个测试文件 (5 条用例) | 仅覆盖 `_build_portfolio_industry_tag` | 至少补充核心路径的单元测试 |
| 4.2 | DataService 无测试 | TypeScript 端零测试 | Vitest / Jest；测试 ProviderChain 降级逻辑 |
| 4.3 | 前端零测试 | 无 Vitest / Vue Test Utils | 关键组件测试 + API 层 mock |
| 4.4 | 无集成/契约测试 | 两服务之间 API 契约未验证 | 针对 `/api/fund/<code>` 等关键端点的契约测试 |

## 5. CI/CD

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 5.1 | 无 CI 流水线 | 无 GitHub Actions 或等效配置 | 添加 CI：lint + typecheck + test + build |
| 5.2 | Python 端无 linter/formatter | DataService 有 `tsc --noEmit`；Python 无 | ruff / black / mypy |
| 5.3 | Docker 仅覆盖 Backend | DataService 和 Frontend 未容器化 | 每个服务独立 Dockerfile；`docker-compose.yml` 编排三服务 |
| 5.4 | 无环境变量校验 | 缺密钥时静默降级，不明确报错 | 启动时校验必填环境变量，缺少时立即退出并提示 |

## 6. 代码质量

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 6.1 | `app.py` 近 7000 行单体 | 路由 + 业务 + 行业分类全部内聚在一个文件 | 按领域拆分为多个 blueprint 模块 |
| 6.2 | 遗留模块仍大量使用 | 5 个标记为 `DEPRECATED` 的模块因 `FUND_DEFAULT_SOURCE=legacy` 默认值而仍活跃 | 完成 DataService 迁移后将默认源切换为 `data_service` 或 `auto` |
| 6.3 | 前端混用 Vue 风格 | 部分 Options API、部分 Composition API | 统一迁移到 `<script setup>` Composition API |
| 6.4 | 前端无 TypeScript | 全部为 JavaScript | 渐进引入 TypeScript；至少类型化 API 响应 |

## 7. DataService 迁移收尾

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 7.1 | 迁移仅完成 ~30% | `/api/fund/<code>` 仍有 19 个阻塞字段未映射 | 逐一实现缺失字段的 DataService provider |
| 7.2 | 缺失 Provider 实现 | `holder_structure`、`same_type_funds`、`risk_metrics`、`scale_fluctuation`、`position_trend`、`total_return_trend` | 在 eastmoney provider 中补全 |
| 7.3 | 业绩板块返回 null | `pingzhongdata` 解析已失效 | 从 EastMoney API 走新路径获取 |
| 7.4 | `akshareMarketProvider` 为空桩 | Node.js 无法调用 Python akshare | 方案 A：Python sidecar；方案 B：用 Node.js SDK 替代 |
| 7.5 | `eastmoneyMarketProvider` 报价/K线未实现 | 这两条路径无 eastMoney 降级 | 实现 `quotes()` 和 `kline()` |

## 8. 前端增强

| 序号 | 问题 | 说明 | 建议方案 |
|------|------|------|----------|
| 8.1 | 搜索无历史/最近查看 | 搜索栏无历史记录展示 | localStorage 持久化最近 10 条搜索 |
| 8.2 | 新闻/板块无分页 UI | FlashNews 固定 30 条，SectorRank 固定 90 条 | 滚动加载或分页按钮 |
| 8.3 | 无离线检测 | 网络断开时无提示 | `navigator.onLine` 事件 + 离线提示 banner |
| 8.4 | 移动端体验差 | 对比面板、拖拽、三列布局在手机端不佳 | 移动端专用布局：单列 + 底部 Tab 导航 |
| 8.5 | 无错误边界 | 未捕获的 Vue 渲染错误可能导致白屏 | `onErrorCaptured` 全局错误边界组件 |
| 8.6 | 降级地址硬编码 | `getWithLocalFallback` 写死 `localhost:5000` | 改用环境变量 `VITE_FALLBACK_API_BASE` |

## 9. 功能完善

| 序号 | 功能 | 说明 |
|------|------|------|
| 9.1 | 基金定投方案生成 | 现有回测能算历史收益，但无法生成最优定投计划（如智能定投、均线策略等） |
| 9.2 | AI 分析流式输出 | 当前 LLM 响应全量缓冲后返回，等待时间长；改为 SSE 流式传输 |
| 9.3 | 基金筛选/对比导出 | 无法将筛选结果或对比数据导出为 CSV/Excel |
| 9.4 | 价格/收益率告警 | 对自选基金设定涨跌幅阈值通知 |
| 9.5 | 市场异动预警 | 大盘涨跌停、成交量突增、板块轮动等自动提醒 |
| 9.6 | i18n 国际化 | 全文硬编码中文，无国际化框架 |
| 9.7 | 深色模式自动跟随系统 | `useTheme.js` 虽支持 auto 模式，但部分组件未完全适配 CSS 变量 |

---

## 优先级建议

```
第一梯队 (P0): 2.1 结构化日志、2.2 健康检查、1.1 速率限制、4.x 测试、5.x CI
第二梯队 (P1): 1.3 密钥安全、3.1 前端状态管理、3.5 Gzip、6.1 app.py 拆分、7.x DataService 迁移收尾
第三梯队 (P2): 6.2 遗留模块下线、3.4 骨架屏、8.x 前端增强、9.3 导出、9.4/9.5 告警
第四梯队 (P3): 9.1 定投方案、9.2 流式 AI、9.6 i18n
```
