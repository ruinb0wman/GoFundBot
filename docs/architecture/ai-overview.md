# AI 分析框架总览

## 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    AI 分析框架                                │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  AI 对话 (Chat)                                     │   │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │   │
│  │  │SkillRouter│→│ ReAct    │→│ SSE Stream        │  │   │
│  │  │ 7 Skills  │  │ Tool Call│  │ token/tool_start  │  │   │
│  │  └──────────┘  │ 7 Tools  │  │ /tool_end/usage   │  │   │
│  │                 └──────────┘  └──────────────────┘  │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  AI 基金分析 (Multi-Agent Debate)                    │   │
│  │  ┌──────────────┐                                    │   │
│  │  │ Performance  │──┐                                 │   │
│  │  │   分析师     │  │                                 │   │
│  │  ├──────────────┤  │  ┌────────────┐                 │   │
│  │  │ Holding      │──┤→│ Supervisor │→ 5 级评分       │   │
│  │  │   分析师     │  │  │  合成      │  Strong Buy→Sell│   │
│  │  ├──────────────┤  │  └────────────┘                 │   │
│  │  │ Manager      │──┤                                 │   │
│  │  │   分析师     │  │                                 │   │
│  │  ├──────────────┤  │                                 │   │
│  │  │ Market       │──┘                                 │   │
│  │  │   分析师     │                                    │   │
│  │  └──────────────┘                                    │   │
│  └──────────────────────────────────────────────────────┘   │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  记忆与反思系统                                       │   │
│  │  storeAnalysis → 7天后 → resolvePending              │   │
│  │  → LLM 反思 / 模板反思 → getPastContext 注入        │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

## 子模块导航

| 模块 | 文档 |
|------|------|
| AI 对话系统 | [AI 对话系统](./ai-chat) |
| AI 基金分析与持仓分析 | [AI 基金分析与持仓分析](./ai-fund-analysis) |
| 记忆与反思系统 | [记忆与反思系统](./ai-memory) |

## LLM 配置体系

```typescript
// settingsService.ts — 内存缓存
interface LLMSettings {
  apiKey: string;
  apiBase: string;      // 默认: https://api.siliconflow.cn/v1
  model: string;         // 默认: deepseek-ai/DeepSeek-R1-Distill-Qwen-32B
}
```

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `apiKey` | — | LLM API 密钥 |
| `apiBase` | `https://api.siliconflow.cn/v1` | OpenAI 兼容 API 端点 |
| `model` | `deepseek-ai/DeepSeek-R1-Distill-Qwen-32B` | 模型名称 |

配置通过前端 Settings 页面 → `PUT /api/settings` → Express 内存缓存 + localStorage 同步。
